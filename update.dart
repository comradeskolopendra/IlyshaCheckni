import 'package:flutter/material.dart';

class UpdateFrame extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Смена пароля'),
        centerTitle: true,
      ),
    );
  }
}